[package]
name = "deadsync-online"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"

[dependencies]
deadsync-net = { path = "../deadsync-net" }
deadsync-score = { path = "../deadsync-score" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"

[lints.clippy]
perf = { level = "warn", priority = -1 }

inline_always = "allow"
