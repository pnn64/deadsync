[package]
name = "deadsync-version"
version = "0.4.667"
edition = "2024"
license = "GPL-3.0"

[dependencies]
log = "0.4.32"
semver = "1.0.28"

[lints.clippy]
perf = { level = "warn", priority = -1 }

inline_always = "allow"
