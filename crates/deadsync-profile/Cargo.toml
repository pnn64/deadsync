[package]
name = "deadsync-profile"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"

[dependencies]
bincode = "2.0.1"
bitflags = "2.13.0"
chrono = "0.4.45"
uuid = { version = "1.18.1", features = ["v4"] }
deadsync-rules = { path = "../deadsync-rules" }
deadsync-score = { path = "../deadsync-score" }

[lints.clippy]
perf = { level = "warn", priority = -1 }

inline_always = "allow"
