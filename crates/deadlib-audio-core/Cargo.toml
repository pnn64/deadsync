[package]
name = "deadlib-audio-core"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"

[dependencies]
rtrb = "0.4.0"


[lints.clippy]
perf = { level = "warn", priority = -1 }

inline_always = "allow"
