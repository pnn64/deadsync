[package]
name = "deadlib-audio-core"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"

[[bench]]
name = "sfx_queue_hot_path"
harness = false

[[bench]]
name = "mixer_hot_paths"
harness = false
required-features = ["bench-support"]

[features]
bench-support = []

[dependencies]
rtrb = "0.4.0"


[lints.clippy]
perf = { level = "warn", priority = -1 }

inline_always = "allow"
