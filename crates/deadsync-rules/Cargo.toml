[package]
name = "deadsync-rules"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"

[dependencies]
deadsync-core = { path = "../deadsync-core" }
log = "0.4.31"

[lints.clippy]
perf = { level = "warn", priority = -1 }

inline_always = "allow"
