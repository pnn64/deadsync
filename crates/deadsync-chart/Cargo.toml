[package]
name = "deadsync-chart"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"

[dependencies]
deadsync-core = { path = "../deadsync-core" }
deadsync-rules = { path = "../deadsync-rules" }
rustc-hash = "2.1.3"

[features]
bench-support = []

[[bench]]
name = "background_expansion_hot_paths"
harness = false
required-features = ["bench-support"]

[lints.clippy]
perf = { level = "warn", priority = -1 }

inline_always = "allow"
