[package]
name = "deadsync-theme"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"

[dependencies]
deadlib-assets = { path = "../deadlib-assets" }

[lints.clippy]
perf = { level = "warn", priority = -1 }

inline_always = "allow"
