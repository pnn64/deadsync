[package]
name = "deadsync-input"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"

[dependencies]
deadsync-core = { path = "../deadsync-core" }
log = "0.4.32"
winit = "0.30.13"

[lints.clippy]
perf = { level = "warn", priority = -1 }

inline_always = "allow"
