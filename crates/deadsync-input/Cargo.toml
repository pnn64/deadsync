[package]
name = "deadsync-input"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"

[dependencies]
log = "0.4.29"

[lints.clippy]
perf = { level = "warn", priority = -1 }

inline_always = "allow"
