[package]
name = "deadlib-audio-backend-telemetry"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"

[dependencies]
deadlib-audio = { path = "../deadlib-audio" }
deadlib-platform = { path = "../deadlib-platform" }
log = "0.4.33"

[lints.clippy]
perf = { level = "warn", priority = -1 }

inline_always = "allow"
