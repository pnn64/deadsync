# Default font loaded behind every top-level bitmap font.
[main]
0=16
1=16

# Match ITGmania: unsupported characters use the first X cell, while invalid
# input uses the second X cell. Local font pages override mapped characters.
map default=0
map invalid=1
