[package]
name = "cfg_aliases"
version = "0.2.1"
edition = "2018"
description = "A tiny utility for defining cfg aliases."
license = "MIT"
repository = "https://github.com/katharostech/cfg_aliases"

[lib]
path = "src/lib.rs"
